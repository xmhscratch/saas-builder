{
    "name": "deploy.gonode",
    "private": true,
    "type": "module",
    "scripts": {
      "build": "docker build -t localhost:5000/gonode:latest ./",
      "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {}
  }
  