const TenantRecord = require('../core/tenant-record')

class UserPrivilege extends TenantRecord {

    // if (!_.isEqual(userId, ownerId)) {
    //     return handleError('User dont have privilege to perform this action')
    // }

}

module.exports = UserPrivilege
